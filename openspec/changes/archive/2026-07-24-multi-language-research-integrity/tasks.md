## 1. Gate de decisión (bloquea el resto)

- [x] 1.1 Llevarle a Cortez la pregunta del constructo: ¿apropiación es transferible entre lenguajes? Presentar las tres salidas del design (covariable / dos variables / recalibración) con su costo respectivo. **Respuesta de Cortez**: misma variable (constructo transferible) — descarta "dos variables".
- [x] 1.2 Registrar la respuesta como ADR nuevo. Verificado libre (057 es el más alto, 054 saltado) → **ADR-058** (`docs/adr/058-apropiacion-constructo-transferible-umbrales-recalibrados-por-lenguaje.md`): salida 3 (recalibración), con la distinción constructo (transferible) vs instrumento/umbrales (recalibrable por lenguaje).
- [~] 1.3 Si la respuesta es "dos variables", agregar al scope el bloqueo de exports mixtos (hoy solo se declara la mezcla, no se rechaza). **NO APLICA**: la decisión fue "misma variable", no "dos variables". Los exports mixtos NO se bloquean (ADR-058).

## 2. Lenguaje en el payload de apertura

- [x] 2.1 Agregar el campo de lenguaje a `EpisodioAbiertoPayload` con default, en `packages/contracts/src/platform_contracts/ctr/events.py:46-81`.
- [x] 2.2 Test que confirme que un evento histórico sin el campo sigue deserializando.
- [x] 2.3 Test golden de `self_hash`: un evento previo al cambio conserva su hash calculado contra el modelo viejo.
- [x] 2.4 Resolver el lenguaje en `tutor-service` al abrir el episodio, desde la respuesta de `AcademicClient` que ya se consulta para validar las 6 condiciones de apertura. Implementado para ambos caminos (Ejercicio del banco vía `get_ejercicio_by_id` y TP monolítica vía `get_tarea_practica_full` — ambos exponen `language` desde epic `java-language-model`, cerrado y verificado contra código real).
- [x] 2.5 Ignorar explícitamente cualquier lenguaje que venga en el cuerpo de la petición, con test que lo cubra.
- [x] 2.6 Test de que reabrir o reanudar un episodio no reescribe el lenguaje del evento de apertura original.

## 3. Verificación de neutralidad sobre la tesis

- [x] 3.1 `uv run pytest apps/classifier-service/tests/unit/test_pipeline_reproducibility.py -v` en verde.
- [x] 3.2 Confirmar que `classifier_config_hash` no cambió para un mismo `(tree_version, reference_profile)`.
- [x] 3.3 Confirmar que `LABELER_VERSION` no cambió.
- [x] 3.4 Verificar la cadena criptográfica de un episodio anterior al cambio y confirmar que pasa sin discrepancias.

## 4. Segmentación en analytics

- [x] 4.1 Resolver el lenguaje por episodio en `analytics-service`. Helper reusable `resolve_episode_languages(ctr_session, tenant_id, episode_ids) -> dict[UUID, str]` en `packages/platform-ops/src/platform_ops/language_segmentation.py` (query batch en `ctr_store`, `payload['language']` del evento `episodio_abierto`, default `"python"` para legacy/sin evento). Exportado desde `platform_ops/__init__.py`. Tests: `packages/platform-ops/tests/test_language_segmentation.py` (11 tests).
- [x] 4.2 Declaración de lenguajes en la respuesta de progresión de cohorte. `CohortProgressionOut.languages_present` en `apps/analytics-service/src/analytics_service/routes/analytics.py` (`get_cohort_progression`), resuelto sobre el episode set post-filtro.
- [x] 4.3 Declaración en concordancia entre evaluadores (kappa). `KappaResponse.languages_present` + `insufficient_data` en `compute_kappa`; el `episode_id` de `KappaRatingIn` (string libre) se resuelve vía UUID cuando parsea, default `python` si no.
- [x] 4.4 Declaración en evolución longitudinal. `CIIEvolutionLongitudinalOut.languages_present` en `get_cii_evolution_longitudinal`.
- [x] 4.5 Declaración en eventos adversariales de cohorte. `CohortAdversarialEventsOut.languages_present` en `get_cohort_adversarial_events`.
- [x] 4.6 Declaración en alertas de estudiante. `StudentAlertsOut.languages_present` en `get_student_alerts` (declarado sobre la cohorte usada para los cuartiles, no solo el estudiante target).
- [x] 4.7 Declaración en distribución de niveles. `NLevelDistributionOut.languages_present` en `get_n_level_distribution` (`GET /episode/{id}/n-level-distribution`, per-episodio — es el único endpoint de "distribución de niveles" que existe en el código; no hay una variante agregada por cohorte). Resuelto sin query extra, reusando el evento `episodio_abierto` que el endpoint ya trae.
- [x] 4.8 Parámetro opcional `language: str | None = None` en los 6 endpoints. Filtro enhebrado en `platform_ops` ANTES de agregar: `RealLongitudinalDataSource._filter_episode_ids_by_language` (helper compartido) aplicado en `list_classifications_grouped_by_student`, `list_adversarial_events_by_comision`, `list_classifications_with_templates_for_student` (`packages/platform-ops/src/platform_ops/real_datasources.py`); `batch_classifications_with_templates_by_student` en `apps/analytics-service/src/analytics_service/services/cohort_slopes.py`; `build_trajectories` en `packages/platform-ops/src/platform_ops/longitudinal.py` reenvía `language` solo si viene seteado (no rompe data sources mock que no conocen el kwarg). Kappa y n-level-distribution filtran inline en la ruta (no hay data-source de por medio).
- [x] 4.9 Test de que un filtro sin resultados devuelve ausencia de datos. A nivel data-source: `packages/platform-ops/tests/test_real_datasources.py` (`test_grouped_by_student_filtro_sin_resultados_devuelve_dict_vacio`, `test_adversarial_events_filtro_sin_resultados_devuelve_lista_vacia`, `test_classifications_with_templates_filtro_sin_resultados`), `apps/analytics-service/tests/unit/test_cohort_slopes_batch.py` (`test_batch_filtro_sin_resultados_devuelve_dict_vacio`). A nivel endpoint: `test_kappa_filtro_sin_resultados_devuelve_ausencia_de_datos` (kappa devuelve `insufficient_data=true`, `kappa=null`, NO `kappa=0.0` fabricado). El patrón general: filtrar ANTES de agregar deja que el input vacío fluya a las funciones de agregación existentes, que ya tratan `[]`/`{}` como "sin datos" (cero conteos, `mean_slope=None`, `insufficient_data`).
- [x] 4.10 Test de que la ausencia del parámetro preserva el comportamiento actual. Cubierto en cada test file de endpoint (`test_analytics_endpoints.py`, `test_f7_endpoints.py`, `test_n_level_distribution_endpoint.py`, `test_cii_evolution_longitudinal_endpoint.py`, `test_adversarial_events_endpoint.py`, `test_student_alerts_endpoint.py`) + a nivel data-source (`test_real_datasources.py::test_grouped_by_student_sin_language_preserva_comportamiento_actual`, `test_classifications_with_templates_sin_language_preserva_comportamiento`).

## 5. Export académico

- [x] 5.1 Incluir el lenguaje por episodio en `packages/platform-ops/src/platform_ops/academic_export.py`. Campo `EpisodeRecord.language` (default `DEFAULT_LANGUAGE`), leído del payload del evento `episodio_abierto` que el loop YA recorre (sin query extra; `or DEFAULT_LANGUAGE` cubre ausente y `null`). En `to_dict` per-episodio. Reusa la constante del helper 4.1 (una sola fuente de verdad para "python").
- [x] 5.2 Declarar los lenguajes presentes en el encabezado del export. `CohortDataset.languages_present` (computado con `languages_present()` del helper 4.1) + en el header de `to_dict`.
- [x] 5.3 Test de que la anonimización vigente no se debilita: el lenguaje no aporta a la reidentificación. `test_5_3_lenguaje_no_debilita_la_anonimizacion` (cohorte mixta: mismo `java` compartido por estudiantes distintos → no es huella individual; sin UUIDs crudos; baja cardinalidad). +`test_5_1_*` y `test_5_2_*`. `packages/platform-ops/tests/test_academic_export.py` → 20 passed.

## 6. Guard de CEC

- [x] 6.1 Extender el contrato de resultados de `cec_features.py` para distinguir tres estados: medido, error transitorio, no aplicable. Implementado como `CECResult.status: CECStatus = Literal["medido","error_transitorio","no_aplicable"]` + campos numéricos ahora `float | None` (None en no_aplicable).
- [x] 6.2 Verificación de lenguaje antes de `ast.parse` (`packages/platform-ops/src/platform_ops/cec_features.py`), devolviendo no-aplicable en vez de los defaults. Guard al inicio de `compute_cec(..., *, language)` contra `SUPPORTED_LANGUAGES = {"python"}`.
- [x] 6.3 Test de que código Java devuelve no-aplicable y **ninguna** puntuación numérica. `test_java_devuelve_no_aplicable_sin_ninguna_puntuacion` + `test_language_es_autoritativo_no_el_parseo`.
- [x] 6.4 Test de regresión: código Python produce resultados idénticos a los previos al cambio. `test_python_default_identico_a_language_explicito` + `test_python_medido_conserva_valores_previos_al_guard`.
- [x] 6.5 Test de que un fragmento Python a medio escribir sigue tratándose como error transitorio y no como lenguaje no soportado. `test_python_a_medio_escribir_es_error_transitorio_no_no_aplicable`.
- [x] 6.6 Test de que un agregado excluye los no-aplicables y declara cuántos excluyó. `aggregate_cec` + `CECAggregate.n_no_aplicable_excluded`; tests `test_aggregate_excluye_no_aplicables_y_declara_cuantos` + `test_aggregate_todo_no_aplicable_da_media_none`.
- [x] 6.7 Confirmar que el guard vive en el módulo y no en los invocadores, con test que llame directo a la función sin pasar por ningún wrapper. `test_guard_vive_en_el_modulo_llamada_directa`.

## 7. Documentación del sesgo

- [x] 7.1 Documentar el confound de calibración donde lo lea quien interpreta los datos, no solo en comentarios de código. Citar `subgrupo.py:14-18` como fuente de la calibración original sobre Pyodide. → `docs/research/sesgo-calibracion-multi-lenguaje.md` (doc para intérpretes de datos; cita `subgrupo.py:14-18`).
- [x] 7.2 Enumerar qué métricas están afectadas y en qué dirección: `dim_experimentacion` a la baja para Java por `EXEC_SCALE`, y las ventanas de `PAUSE_THRESHOLD` (5min) y `CORRELATION_WINDOW` (2min) infladas por latencia de compilación. → tabla en el doc; `dim_experimentacion` y `CCD_mean` a la baja (grounded en fórmula: `subgrupo.py:135-137` y `ccd.py:188`), CT fragmentada por `PAUSE_THRESHOLD` (mecanismo), CEC `no_aplicable`.
- [x] 7.3 Referenciar el ADR de la decisión de constructo de la tarea 1.2. → el doc referencia ADR-058 como marco constructo/instrumento.

## 8. Cierre

- [x] 8.1 Smoke test que abra un episodio sobre un ejercicio Java y verifique que el lenguaje llega al evento de apertura. **EJECUTADO Y VERDE (2026-07-24)**: `tests/e2e/smoke/test_smoke_language_provenance.py::test_episodio_sobre_tp_java_registra_java_en_el_evento_de_apertura` — crea ejercicio+TP Java, publica, abre episodio como estudiante, poll al alias `GET /api/v1/audit/episodes/{id}` (15s) y verifica `payload["language"] == "java"` en el evento de seq=0. Corrido contra el stack real (11 servicios + 8 partition workers + Postgres 16 en container).
- [x] 8.2 Smoke test de un endpoint de analytics sobre cohorte mixta, verificando la declaración de ambos lenguajes. **EJECUTADO Y VERDE (2026-07-24)**: mismo archivo, `test_analytics_sobre_cohorte_mixta_declara_ambos_lenguajes` + `test_filtro_por_lenguaje_recorta_la_cohorte_mixta`. Cohorte mixta = episodio Java de 8.1 + episodio legacy del seed (payload sin campo `language` → default `python`, que es la garantía de comparabilidad con el piloto-1). Se eligió `POST /api/v1/analytics/kappa` entre los 6 endpoints con `languages_present` porque es el único donde el conjunto de episodios viaja en el request — permite una cohorte mixta determinística sin depender de que el classifier haya corrido sobre el episodio nuevo. Cubre las dos caras: declaración sin filtro (`["java","python"]`) y filtro `?language=` recortando de verdad (+ filtro sin resultados → `insufficient_data`, NO `kappa=0.0`).
  - Suite smoke completa en verde con los 3 tests nuevos incluidos: **77 passed, 1 skipped, 0 failed** (el skip es `test_smoke_byok.py:106`, esperado — `BYOK_MASTER_KEY` no configurada en dev).
  - Requisitos de entorno destrabados para poder correrlos (ninguno es cambio de código de la change): servicio Windows `postgresql-x64-18` parado (se robaba `127.0.0.1:5432` al container por exact-match); migración `20260723_0001_ejercicio_tp_language` aplicada sobre `academic_main`; 206 policies Casbin sembradas; `.env` creado desde `.env.example` (gitignored); api-gateway relanzado con `DEV_TRUST_HEADERS=true`; `governance-service` con `PROMPTS_REPO_PATH` en ruta nativa Windows; `ai-gateway` con las API keys de proveedores vacías; directorio `attestations/` creado (gitignored).
- [x] 8.3 `make test-fast` en verde. **1456 passed, 4 skipped, 0 failed** (2026-07-23). Destrabado por un fix ajeno a esta change: `apps/governance-service/tests/unit/test_prompt_loader.py::_make_repo` escribía el `system.md` temporal con `write_text()` sin `encoding=`, o sea cp1252 en Windows, y `PromptLoader.load()` lo lee con `encoding="utf-8"` fijo → `UnicodeDecodeError` sobre la tilde de "socrático". Bug preexistente y Windows-only (en el CI Linux el default ya es utf-8, por eso nunca se vio). El código de producción está bien; el que estaba mal era el test. Va en commit aparte.
- [x] 8.4 Actualizar `CLAUDE.md` si cambió el conteo de smoke tests (lo verifica `scripts/check-claude-md.py` en CI). Smoke: 42 → **45** (los 3 nuevos de 8.1/8.2). El gate destapó un segundo drift que también es de esta change: ADRs 57 → **58** por el ADR-058 de la tarea 1.2. `uv run python scripts/check-claude-md.py` → `[OK] 3 claims verificados, 2 skip, sin drift`.
