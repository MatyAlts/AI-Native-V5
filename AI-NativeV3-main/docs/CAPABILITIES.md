# Capabilities cerradas y en curso

## Capabilities cerradas en epic `ai-native-completion-and-byok`

5 capabilities backend con ADRs 033-040 (los detalles operativos viven en los invariantes y constantes de `CLAUDE.md` — las constantes hash, `LABELER_VERSION=1.2.0`, `BYOK_MASTER_KEY`, `_EXCLUDED_FROM_FEATURES` ya están documentadas):

- **Reflexión metacognitiva post-cierre** (ADR-035, RN-133): `POST /episodes/{id}/reflection` emite `reflexion_completada` excluido del classifier. Export académico redacta textuales por default; flag `--include-reflections` con audit log.
- **Sandbox client-side + test_cases** (ADR-033, ADR-034, RN-134): JSONB en `tareas_practicas[_templates]`, filter por rol en GET, `POST /run-tests` solo conteos (no código). Pyodide diferido al piloto-2.
- **TP-gen IA** (ADR-036): `POST /tareas-practicas/generate` en academic-service → governance (prompt `tp_generator/v1.0.0`) → ai-gateway con `materia_id` para BYOK. Audit log structlog `tp_generated_by_ai`. Schema `TareaPracticaCreate.created_via_ai: bool`. Wizard UI deferido.
- **Governance UI admin** (ADR-037): `/cohort/{id}/adversarial-events` + `/governance/events` cross-cohort en analytics-service. `GovernanceEventsPage.tsx` en web-admin con cascade filters + CSV cp1252-safe.
- **BYOK multi-provider** (ADR-038/039/040, RN-132): AES-256-GCM con `BYOK_MASTER_KEY`, resolver jerárquico **materia → tenant → env_fallback**, 5 endpoints CRUD. Casbin `byok_key:CRUD` para superadmin/docente_admin. `materia_id` cacheado en `SessionState` (no re-resuelve por turno). Métricas OTLP `byok_key_resolution_total{resolved_scope}` (SLO p99 < 50ms). Mistral adapter implementado (Gemini diferido, UI BYOK page diferida, cache Redis diferido).

**Anti-regresión crítica del epic**: el classifier consumía TODOS los eventos del CTR sin filtro. `reflexion_completada` post-cierre cambiaba `ct_summary` (0.54 → 0.56). Fix en `_EXCLUDED_FROM_FEATURES` + test `test_reflexion_completada_no_afecta_clasificacion_ni_features` en `apps/classifier-service/tests/unit/test_pipeline_reproducibility.py`. **NO romper este test** al agregar event types nuevos.

## Capabilities cerradas en sprint `plan-mejora-instrumentos-research` (2026-05-17)

Cierran P1 + P2-4 + P2-1/2/3 del `PlanMejora.md` (root del wrapper `AI-Native-V4-main/`). Materializan las brechas conceptuales del paper Cortez & Garis (`paper_conaiisi.pdf`, versión 2026-05-16) + 3 instrumentos del diseño cuasi-experimental del paper §6.2.

- **Marcos interpretativos MI1-MI3 + protocolo interpretativo de 7 principios + marcador anti-reificación en UI** (ADR-053): documentación canónica de los tres marcos interpretativos del paper §4.3 (calidad epistémica, apropiación reflexiva en sentido fuerte, coherencia estructural multidimensional como horizonte evaluativo) + 7 principios del paper §7.3 (contextualidad, no-reducción, triangulación obligatoria, no-etiquetado individual, no-uso para alto stake sin triangulación, explicitación de versión, apertura a la crítica). Marcador "Perfil tipologico de apropiación" implementado en `apps/web-teacher/src/utils/docenteLabels.ts` con helper `appropriationWithScope` + constante `APPROPRIATION_REIFICATION_DISCLAIMER` renderizada en `ProgressionView` y `StudentLongitudinalView` (modo investigador).
- **Cinco riesgos a priori del diseño cuasi-experimental documentados** (`docs/limitaciones-declaradas.md` versión 2026-05-17): R1 efecto Hawthorne, R2 performatividad cognitiva (confound intervención-medición del paper §6.3), R3 deriva LLM, R4 brecha digital previa, R5 sobrecarga cognitiva. Cada uno con mitigación operacional y archivos del repo que la materializan.
- **Protocolo de entrevistas semi-estructuradas para triangulación cualitativa** (`docs/research/protocolo-entrevistas-piloto.md`): framework Braun & Clarke (2006) análisis temático reflexivo (sin kappa sobre temas per Braun & Clarke 2019), muestreo estratificado, 3 bloques (reconstrucción episodio + percepción tutor + percepción IA), procesamiento en 6 fases, triangulación explícita con MI1 y MI2 del ADR-053. Pendiente revisión coautoral con Ana Garis + comité ético UNSL.
- **Cuestionario sobre experiencia previa con IA** (esqueleto técnico, P2-2): tabla `respuestas_cuestionario_ia` con RLS forzado + UNIQUE idempotente, schema Pydantic, route POST/GET `/me`/GET `/catalogo`/GET `/{comision}/summary` con k-anonymity gate `MIN_STUDENTS_FOR_COHORT_SUMMARY=5`, 7 policies Casbin nuevas. Frontend en `apps/web-student/src/pages/InstrumentosPage.tsx` (Card 1) + vista cohorte agregada en `apps/web-teacher/src/views/InstrumentosCohorteView.tsx`. Contenido marcado `[PLACEHOLDER GARIS]`; bumpear `instrument_version` cuando contenido aprobado.
- **Pretest de autoeficacia en programación** (esqueleto técnico, P2-1): tabla `respuestas_pretest_autoeficacia` con scoring server-side (total + sub-escalas por promedio). Basado en Lishinski et al. (2016) CS Self-Efficacy Scale, adaptación al castellano rioplatense v0.1.0-draft. Cuando Garis valide el contenido, se inyecta sin tocar plumbing. 12 items placeholder distribuidos en 4 sub-escalas (independencia, complejidad, aprendizaje, persistencia) para validar el flujo end-to-end; los 28 finales llegan post-aprobación.
- **Test de transferencia (medida dependiente H2)** (esqueleto técnico, P2-3): tabla `respuestas_test_transferencia` con CheckConstraint `group_assignment IN ('experimental', 'comparison')` + UNIQUE por (tenant, comision, student, test_id, version). Endpoint agregado `/summary` por grupo (k-anonymity gate aplicado POR GRUPO — experimental y comparison se reportan independientes). 3 problemas placeholder en `instrumentos_content.py`; el draft `docs/research/diseno-test-transfer.md` propone 5 finales tras validación cátedra.
- **Migration `20260517_0001_instrumentos_research`** con las 3 tablas + RLS + UNIQUEs + indexes.
- **Seed `scripts/seed-instrumentos-respuestas.py`**: idempotente con `ON CONFLICT DO NOTHING`. Inserta respuestas placeholder de los 18 estudiantes del seed-3-comisiones → 18 cuestionario_ia + 18 pretest + 54 transferencia = 90 inserts. Habilita probar el dashboard cohorte con N=6 por comision (suficiente para pasar k-anonymity).
- **Rutas TanStack navegables**: web-student `/instrumentos?comisionId=...` (link en `materia.$id.tsx` ContextualHeader) + web-teacher `/instrumentos-cohorte?comisionId=...` (link en sidebar Análisis con icono `FlaskConical`).
- **Casbin policies +21**: 3 recursos (`instrumento_cuestionario_ia`, `instrumento_pretest_autoeficacia`, `instrumento_test_transferencia`) × 7 acciones cada uno (3 superadmin C/R/D + 1 docente_admin R + 1 docente R + 2 estudiante C/R). Total Casbin = 205 (era 184).
- **Tests unit**: `apps/academic-service/tests/unit/test_instrumentos.py` con cobertura de schemas (Create/Out) + validación de respuestas + scoring del pretest + catálogo + assertion explícita `test_catalogo_tiene_marcadores_placeholder` que evita que un item placeholder se cuele como contenido validado.

**Estado del contenido académico** (gate humano):

| Instrumento | Esqueleto técnico | Contenido v0.1.0-draft | Validación pendiente |
|---|---|---|---|
| Cuestionario IA previa | ✅ | 8 items con `[PLACEHOLDER GARIS]` | Ana Garis + comité ético UNSL |
| Pretest autoeficacia | ✅ | 12 items con `[PLACEHOLDER GARIS]` (extracto de Lishinski 2016) | Ana Garis + traducción castellano rioplatense + comité ético UNSL |
| Test transferencia | ✅ | 3 problemas con `[PLACEHOLDER CATEDRA UNSL]` | Cátedra UNSL valida 5 problemas finales + patrones de evaluación automática |

**Anti-regresión crítica del sprint**: el archivo `instrumentos_content.py` marca cada item con literal `[PLACEHOLDER GARIS]` o `[PLACEHOLDER CATEDRA UNSL]`. El test `test_catalogo_tiene_marcadores_placeholder` afirma que estos markers siguen presentes mientras el contenido NO esté validado. Cuando Garis o la cátedra aprueben items específicos, deben sacar el marker manualmente y el test va a fallar para esos items — esto es la señal explícita de que el contenido pasó a validado y que el sprint final puede correrse contra esa versión. NO bumpear `instrument_version` antes de sacar los markers; NO sacar los markers sin aprobación coautoral.

**Trazabilidad ejecutada del sprint**: ver `docs/SESSION-LOG.md` entrada `## 2026-05-17` — typecheck + lint + ast.parse pasaron 100% en archivos del sprint. Trazabilidad pendiente requiere stack bootstrapped (`make migrate` + `casbin_policies` re-seed + `seed-instrumentos-respuestas` + `pytest test_instrumentos.py` + `make test-smoke` + `make check-rls`).
